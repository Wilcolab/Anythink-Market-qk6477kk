/**
 * Converts a string to camelCase format.
 * 
 * @param {string} str - The input string to convert
 * @returns {string} The converted string in camelCase
 * 
 * @example
 * toCamelCase("hello world") // returns "helloWorld"
 * toCamelCase("first_name") // returns "firstName"
 * toCamelCase("background-color") // returns "backgroundColor"
 * toCamelCase("THIS IS SPARTA") // returns "thisIsSparta"
 * 
 * Notes:
 * - Handles spaces, hyphens, and underscores as word separators
 * - Removes all separators and capitalizes the first letter of each word (except first)
 * - Converts all characters to lowercase before processing
 * - Returns empty string if input is null or undefined
 */
function toCamelCase(str) {
    // Implementation goes here
}